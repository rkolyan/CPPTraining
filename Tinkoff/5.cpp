//
// Created by rkolyan on 27.01.2025.
//

#include <list>
#include <set>
#include <queue>
#include <map>
#include <iostream>

int main(void) {
    std::size_t n = 0, m = 0, u = 0, v = 0, ch = 0;
    std::vector<std::vector<std::pair<int, unsigned char>>> matr;

    std::cin >> n >> m;
    matr.resize(n);
    for (auto &vec : matr) {
        vec.resize(m);
        std::fill(vec.begin(), vec.end(), std::pair<int, unsigned char>(-1, 0));
        if ()
    }
    for (std::size_t i = 0; i < n; i++) {
    }
    return 0;
}